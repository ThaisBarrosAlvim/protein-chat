bind = "0.0.0.0:5000"
workers = 2
timeout = 300
loglevel = "debug"  # Nível de log: "debug", "info", "warning", "error", "critical"
accesslog = "-"  # Para exibir os logs de acesso no stdout (ou especifique um arquivo)
errorlog = "-"  # Para exibir os logs de erro no stdout (ou especifique um arquivo)
